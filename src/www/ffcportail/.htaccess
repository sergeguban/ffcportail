<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteBase /ffcportail/ffcportail
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
